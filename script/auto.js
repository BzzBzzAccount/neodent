let myBtn = document.querySelector('.btn')

class Users{
    constructor(userName, password){
        this.userName = userName
        this.password = password
    }
    validPass(){
        if(this.userName === 'neo' && this.password === 'dentica'){
            href=''
        }
    }
}